/* function saudacao(nome) {
    return `Bom dia ${nome}!`;
};

const variavel = saudacao('Luiz');
console.log(variavel); */

/* function soma(x, y) {
    const resultado = x + y;
    return resultado;
};

console.log(soma(2, 2)); */

// Declarando uma função dentro de uma variável
/* const raiz = function (n) {
    return Math.sqrt(n); // Retorna a raiz quadrada de n
};

console.log(raiz(9)); */

// Segundo método para declarar uma função dentro de uma variável
// const raiz = n => Math.sqrt(n);