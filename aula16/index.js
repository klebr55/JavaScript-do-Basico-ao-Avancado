const alunos = ['Luiz', 'Maria', 'João'];

alunos.slice(0, 3); // Corta os arrays no alcance definido

/* delete alunos[1]; // Deleta um elemento do Array e deixa um espaço vazio no lugar dele
console.log(alunos); */

// alunos.pop(); // Remove elementos do final do Array
// alunos.shift(); // Remove elemntos do começo do Array

/* const removido = aluno.pop(); // Salva elementos removidos na constante "removido"
console.log(removido);
console.log(alunos); */

/* alunos.unshift('Fábio'); // Adiciona no começo
alunos.unshift('Luiza'); // Adiciona no começo */

/* alunos.push('Otávio'); // Adiciona no fim
alunos.push('Fábio'); // Adiciona no fim */

/* alunos[alunos.length] = 'Luiza'; // Adiciona no fim
alunos[alunos.length] = 'Fábio'; // Adiciona no fim
alunos[alunos.length] = 'Luana'; */ // Adiciona no fim

// console.log(alunos);

// alunos[0] = 'Eduardo';
// alunos[1] = 'Luiza';

// console.log(alunos);
